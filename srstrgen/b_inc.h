#include <string>
#include <cstring>
#include <algorithm>
#include <iostream>
using std::cout;

using std::string;
using std::sort;

string b_incs(const char*);
string add_cs(string &I,char *E);
string remove_excs(string&, char*);
